* Add a generator for the bytecode => direct code translator
* add indirect dispatch approach
* add some different prefetch strategies
* add some stack element caching
* fix types of stack vs types of elements blarg
* add super instructions
* add use of LIKELY/UNLIKELY macros
* make use of PREFETCH macro


OSX tools:
* nm (which spits out symbol names) 
* otool (which disassembles and gives other information about a binary). i.e. otool -Vt 
